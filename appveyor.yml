version: 1.5.{build}
image: Visual Studio 2017
configuration: Release
nuget:
  project_feed: true
before_build:
  - nuget restore
build:
  publish_nuget: true
  verbosity: minimal
artifacts:
- path: Artifacts
  name: NuGet
